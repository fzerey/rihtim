; Use a flat, hoisted node_modules layout (like npm/yarn).
; Required so Next.js standalone output includes all of `next`'s dependencies
; (e.g. styled-jsx) and to avoid pnpm symlinks breaking desktop packaging.
node-linker=hoisted
